import { CharacterPipePipe } from './character-pipe.pipe';

describe('CharacterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
